{{!--
    Parameters:
    * action (button, search)
--}}

<section class="verdin-header verdin-outer">
    <div class="verdin-header-inner">
       
            <div class="verdin-head-brand">
                <a class="verdin-head-logo{{#unless @site.logo}} no-image{{/unless}} h2 bolder" href="{{@site.url}}">
                    {{#if @site.logo}}
                        <img src="{{@site.logo}}" alt="{{@site.title}}">
                    {{else}}
                        {{@site.title}}
                    {{/if}}
                </a>
                <nav class="verdin-head-menu">
                    {{navigation}}      
                </nav>   
                <button class="verdin-burger"></button>
            </div>   

            <div class="verdin-head-actions">
            
                   
                    <div class="verdin-head-members">
                        {{#unless @member}}
                            {{#unless @site.members_invite_only}}
                                <a class="verdin-head-link" href="#/portal/signin" data-portal="signin">Sign in</a>
                                <a class="verdin-head-btn verdin-btn verdin-primary-btn" href="#/portal/signup" data-portal="signup">Subscribe</a>
                            {{else}}
                                <a class="verdin-head-btn verdin-btn verdin-primary-btn" href="#/portal/signin" data-portal="signin">Sign in</a>
                            {{/unless}}
                        {{else}}
                            <a class="verdin-head-btn verdin-btn verdin-primary-btn" href="#/portal/account" data-portal="account">Account</a>
                        {{/unless}}
                    </div>

            </div>
            

      

    </div>
</section>

<section class="verdin-sidebar-header">
    <button class="verdin-search verdin-icon-btn" aria-label="Search this site" data-ghost-search>{{> "icons/search"}}</button>
</section>